# tax_rules.js + tax_engine.js v0.3-B 합본 작업지시서

| 항목 | 내용 |
|---|---|
| 문서 ID | `docs/05_code_work_orders/07_v0_3_B.md` |
| 버전 | v0.3-B (확정신고 v3 산식 — 법 제104조 ⑤ 정확본, 의사결정 #13) |
| 작성 일자 | 2026-05-06 |
| 작성 출처 | 작업 창 #16 (작업지시서 v0.3-B 합본 산출 단일 책임) |
| 산출 형식 | **합본 단일 산출** (사용자 결정 (A) 채택, 5/6 단계 1 보고 검토 후) |
| 대상 코드 | `js/tax_rules.js` + `js/tax_engine.js` (v0.3-A → v0.3-B 패치) |
| 대상 테스트 | `tests/tax_rules.test.js` + `tests/tax_engine.test.js` (v0.3-A → v0.3-B 패치) |
| 의존 모듈 스펙 | `docs/v0.3/modules/tax_rules.md` v0.3-B (1,401줄, 작업 창 #15 산출) + `docs/v0.3/modules/tax_engine.md` v0.3-B (1,303줄, 작업 창 #15 산출) |
| 의존 명세서 | `docs/v0.3/02_calc_engine_spec.md` v0.3-B (§5-7 5단계 산식 단일 진본) |
| 의존 의사결정 | `docs/99_decision_log.md` #13 (확정신고 v3 산식 — 법 제104조 ⑤ 정확본) |
| 의존 골든셋 | `docs/v0.3/04_test_cases_manual_v03B_step6.xlsx` 시트 19 (산식정정_v3) — TC-S05·S06·S07 v3 정답값 |
| 이전 버전 | v0.3-A 분리본 (`05_tax_rules_v0_3.md` 998줄 + `06_tax_engine_v0_3.md` 1,401줄, 5/2 KPI 100% 통과) |
| 패치 라인 추정 | `tax_rules.js` +3~+4 라인 + `tax_engine.js` +250~+350 라인 = 합계 +253~+354 라인 |
| 회귀 안전성 | TC-001~014 14건 totalTax 100% 일치 보존 (의사결정 #13 `SINGLE_TRANSFER` 분기 영역) |

---

## §1. 산출 목표·전제

### §1-1. 산출 목표

본 작업지시서는 v0.3-A → v0.3-B 패치를 처리한다. 패치 영역 4종:

1. `js/tax_rules.js` v0.3-B 패치 (약 +3~+4 라인) — `RULE_VERSION` 1라인 갱신 + `LAW_REFS.finalReturnAggregation` 1키 추가
2. `js/tax_engine.js` v0.3-B 패치 (약 +250~+350 라인) — 확정신고 v3 산식 5단계 본문 + 누진 헬퍼 공개 노출 격상 + `result.steps` 신규 4종 + issueFlag 신규 2종 + `ENGINE_VERSION` 1라인 갱신
3. `tests/tax_rules.test.js` v0.3-B 패치 — `RULE_VERSION` strict-eq 1라인 갱신
4. `tests/tax_engine.test.js` v0.3-B 패치 — `ENGINE_VERSION` strict-eq 1라인 갱신 + TC-S05·S06·S07 v3 신규 회귀 그룹 추가

### §1-2. 전제 — 회귀 안전성 (절대 깨지면 안 됨)

| 영역 | 검증 |
|---|---|
| TC-001~014 14건 totalTax | v0.3-A 정답값 100% 일치 |
| v0.3-A `tax_rules.js` 26종 시그니처 | 변경 0건 (`RULE_VERSION` 1라인만 예외) |
| v0.3-A `tax_engine.js` 21종 시그니처 | 변경 0건 (`ENGINE_VERSION` 1라인만 예외) |
| 13단계 단일 양도 파이프라인 | 본문 변경 0건 (양도 1건당 산식은 v0.3-A 그대로) |
| `selfTest` | v0.3-A 7종 모두 `ok === true` 보존 |
| Node.js 회귀 baseline | v0.3-A 통과 후 → v0.3-B 추가 후 변경 0건 (RULE_VERSION strict-eq 1라인만 갱신) |

> **회귀 깨지면 즉시 롤백**: 본 작업지시서 산출 코드가 v0.3-A 결과를 보존하지 못하면 v0.3-B 마이그레이션 실패. 의사결정 #11 (정확성 > 속도) 적용.

### §1-3. 의사결정 #13 본문 직접 인용

법 제104조 ⑤ 정확본:

> 해당 과세기간에 제94조제1항제1호·제2호 및 제4호에서 규정한 자산을 둘 이상 양도하는 경우 양도소득 산출세액은 다음 각 호의 금액 중 큰 것으로 한다.
> 1. 해당 과세기간의 양도소득과세표준 합계액에 대하여 제55조제1항에 따른 세율을 적용하여 계산한 양도소득 산출세액
> 2. 제1항부터 제4항까지 및 제7항의 규정에 따라 계산한 자산별 양도소득 산출세액 합계액. 다만, 둘 이상의 자산에 대하여 제1항 각 호, 제4항 각 호 및 제7항 각 호에 따른 세율 중 동일한 호의 세율이 적용되고, 그 적용세율이 둘 이상인 경우 해당 자산에 대해서는 각 자산의 양도소득과세표준을 합산한 것에 대하여 제1항·제4항 또는 제7항의 각 해당 호별 세율을 적용하여 산출한 세액 중에서 큰 산출세액의 합계액으로 한다.

### §1-4. 5/4 합의 결정 5건 영속화 위치

| # | 결정 영역 | 본 작업지시서 §X 영속화 위치 |
|---|---|---|
| 1 | `getRateGroupKey` 정수 키 (`Math.round(addition * 100)`) — B-022 부동소수점 회피 | §4-3-3 (헬퍼 함수 본문) |
| 2 | clause2 단서 산식 — v0.3-A 누적 baseTax 산식 그대로 (Python 표준 누진공제 산식 미사용) | §4-2-2 (`findHeavyProgressiveTaxAmount` 공개 노출 + §4-3-4 호출 영역) |
| 3 | `applyFinalReturnV3`의 `length === 1` → `SINGLE_TRANSFER` 분기 — TC-001~014 회귀 보존 | §4-3-5 (4단계 의사코드 분기 본문) |
| 4 | issueFlag 신규 2종 (`FINAL_RETURN_AGGREGATE_PROGRESSIVE_APPLIED` + `FINAL_RETURN_DAN_SEO_APPLIED`) | §4-5 (issueFlag 카탈로그) + §4-6 (`collectIssueFlags` 본문) |
| 5 | RULE_VERSION "v0.3.1-final-return-v3" + ENGINE_VERSION "v0.3.0-B" | §3-2-1 + §4-1 |

### §1-5. 시행령 인용 폐기 영역 (사용자 39번째 짚음 정정)

`LAW_REFS.finalReturnAggregation` 영역은 **시행령 제167조의10 인용 0건**이다. 시행령 제167조의10은 다주택 중과 자산 정의 조문일 뿐이며 `LAW_REFS.heavyTaxation` 키에서만 인용된다 (v0.3-A 본문 그대로 보존).

본 작업지시서 본문 + §10 부록 B 자체 검증 영역에 시행령 인용 0건 영속화 필수.

### §1-6. 본 작업지시서 단일 책임

| 영역 | 본 작업지시서 책임 |
|---|---|
| `tax_rules.js` + `tax_engine.js` v0.3-B 패치 작업지시서 | ✅ 단일 책임 |
| `.js` 본문 산출 | ❌ Claude Code 단일 책임 (의사결정 #9 v9) |
| 모듈 스펙 갱신 | ❌ 작업 창 #15 단일 책임 (완료) |
| 의사결정 로그 갱신 | ❌ 사용자 영속화 단일 책임 (의사결정 #6) |
| 시나리오 엔진 (`scenario_engine.js`) 작성 | ❌ 후속 작업 창 #17+ 인계 |
| 골든셋 갱신 | ❌ 검증 후 별도 작업 창 인계 |

---

## §2. 작업 전 정독 자료

본 §2는 Claude Code가 본 작업지시서 진입 전에 정독해야 할 자료를 정의한다.

### §2-1. 1순위 정독 자료 (모듈 스펙·명세서·의사결정 — 산식 단일 진본)

| # | 자료 경로 | 정독 영역 | 본 작업지시서 인용 위치 |
|---|---|---|---|
| 1 | `docs/v0.3/modules/tax_rules.md` v0.3-B (1,401줄) | §0 변경 요약 + §3-6-1-B (RULE_VERSION 갱신) + §3-6-2 (LAW_REFS 12종 표) + §3-B (rules 측 단일 책임) + 부록 A·B | §3 전체 |
| 2 | `docs/v0.3/modules/tax_engine.md` v0.3-B (1,303줄) | §0 변경 요약 + §2 노출 멤버 23종 + §4-2-2-B (`result.steps` 신규 4종) + **§5-7 5단계 산식 5종 함수 계약** + §6-B issueFlag 카탈로그 27종 + 부록 B | §4 전체 |
| 3 | `docs/v0.3/02_calc_engine_spec.md` v0.3-B | §5-7-1·§5-7-2·§5-7-3 (5단계 산식 본문) + §10-6·§10-7 (TC-S05·S06 본문) | §4-3 + §6-3 |
| 4 | `docs/99_decision_log.md` #13 | 결정 사유 + 회귀 안전성 + 시행령 폐기 본문 | §1-3 + §1-5 |
| 5 | 소득세법 법 제104조 ⑤ (PDF) | 본문·1호·2호 본문·2호 단서 정확본 | §1-3 |

### §2-2. 2순위 정독 자료 (골든셋 정답값 — 회귀 그룹 본문)

| # | 자료 경로 | 정독 영역 | 본 작업지시서 인용 위치 |
|---|---|---|---|
| 6 | `docs/v0.3/04_test_cases_manual_v03B_step6.xlsx` 시트 19 (산식정정_v3) | TC-S05 4 시나리오 + TC-S06 6 시나리오 + TC-S07 분산 시나리오 v3 정답값 (Σ totalTax 9건) | §6-3 (R-B 회귀 그룹 본문) |

> **자료 6 정독 정책**: `tax_engine.md` v0.3-B §5-7-6 영속화 본 인용 그대로 사용. 작업 창 #15 정독 성공 영속화 영역 인계 — 본 작업지시서는 영속화 본을 단일 진본으로 인용.

### §2-3. 3순위 정독 자료 (작업지시서 패턴 단일 진본)

| # | 자료 경로 | 정독 영역 | 본 작업지시서 인용 위치 |
|---|---|---|---|
| 7 | `docs/05_code_work_orders/v0_3_A/05_tax_rules_v0_3.md` (998줄) + `06_tax_engine_v0_3.md` (1,401줄) | §섹션 구조 + §변경 영역 본문 + §검증 체크리스트 + 부록 자체 검증 | 본 작업지시서 §섹션 구조 전체 |

### §2-4. 정독 우선순위 (Claude Code 작업 진입 직전)

1. **자료 1·2** (모듈 스펙 v0.3-B Part 1·Part 2) → §3·§4 본문 단일 진본
2. **자료 3** (명세서 v0.3-B §5-7) → 산식 5단계 본문 검증
3. **자료 4** (의사결정 #13) → 회귀 안전성 영역 본문 검증
4. **자료 5** (법 제104조 ⑤) → 1호·2호 본문 정확본 검증
5. **자료 6** (xlsx 시트 19) → R-B 회귀 그룹 정답값 검증
6. **자료 7** (v0.3-A 분리본) → §섹션 구조 패턴 검증

> **정독 의무**: 본 6단계 모두 정독 후 §3 진입. 정독 누락 시 산출 코드 회귀 깨질 가능성. 의사결정 #11 (정확성 > 속도) 적용.

---

## §3. tax_rules.js v0.3-B 패치 (약 +3~+4 라인)

### §3-1. v0.3-A 시그니처 보존 (26종 그대로)

v0.3-A의 26종 노출 멤버는 **모두 시그니처·값 그대로 보존**한다. 본 작업지시서는 26종 일괄 표를 모듈 스펙 `tax_rules.md` v0.3-B §2-2 + v0.3-A §2-2를 그대로 인용하여 단순 옮긴다 (Claude Code는 정독 후 적용).

| 카테고리 | 멤버 수 | 본 패치 영향 |
|---|---|---|
| 룩업 테이블 | 5종 (`PROGRESSIVE_BRACKETS` 8행 + `LONG_TERM_DEDUCTION_TABLE_1` 13행 + `_TABLE_2_HOLDING` 8행 + `_TABLE_2_RESIDENCE` 9행 + `HEAVY_TAX_RATE_ADDITION` 2행) | 변경 0건 |
| 룩업 함수 | 4종 (`findBracket` + `findHoldingRate` + `findResidenceRate` + `findHeavyTaxRateAddition`) | 변경 0건 |
| 상수 | 9종 (`UNREGISTERED_RATE`·`SHORT_TERM_RATE_*`·`HIGH_VALUE_THRESHOLD`·`BASIC_DEDUCTION_AMOUNT`·`LOCAL_INCOME_TAX_RATE`·`MIN_RESIDENCE_MONTHS`·`MIN_HOLDING_YEARS_FOR_LONG_TERM`·`HEAVY_TAX_PROGRESSIVE_BASE_RATE`) | 변경 0건 |
| 메타데이터 | 4종 (`RULE_VERSION` + `LAW_REFS` + `selfTest` + `getEffectiveRules`) | **`RULE_VERSION` 1라인 갱신 + `LAW_REFS` 1키 추가** |
| **합계** | **26종 (그대로)** | **변경 1라인 + 추가 1키** |

> **인터페이스 약속 (가장 중요)**: v0.3-A의 26종 노출 멤버 시그니처·값·반환 형식 변경 0건. v0.3-B 패치는 **순수 추가** (addition-only)이며 v0.3-A·v0.2·v0.1 회귀 (TC-001~014 14건)를 깨지 않는다.

### §3-2. 패치 영역 일람

#### §3-2-1. RULE_VERSION 갱신 (1라인)

```js
// v0.3-A:
const RULE_VERSION = "v0.3.0-post-20260510";

// v0.3-B (1라인 갱신):
const RULE_VERSION = "v0.3.1-final-return-v3";
```

**갱신 사유**: 의사결정 #13 — 확정신고 v3 산식 정확 도입. 동일 과세기간 다중 양도 시 법 제104조 ⑤ 정확본 산식 (1호 합산 누진 + 2호 단독 합계 + 단서) 정확 적용.

**명명 규칙 영속화**: `"v0.3.1-final-return-v3"` 의미:
- `v0.3.1`: v0.3 라인의 패치 번호 (`v0.3.0` = v0.3-A 정본, `v0.3.1` = v0.3-B 패치본)
- `final-return-v3`: 확정신고 v3 산식 정확 도입 식별

#### §3-2-2. LAW_REFS.finalReturnAggregation 1키 추가 (2~3라인)

```js
// v0.3-A LAW_REFS (11종):
const LAW_REFS = Object.freeze({
  incomeTaxAct: "소득세법 [법률 제21065호, 2026-01-02 시행]",
  incomeTaxEnforcement: "소득세법 시행령 [대통령령 제36129호, 2026-03-01 시행]",
  progressiveRate: "소득세법 제55조 제1항",
  transferTaxRate: "소득세법 제104조 제1항",
  basicDeduction: "소득세법 제103조",
  localIncomeTax: "지방세법 제103조의3",
  nonTaxation1Se1House: "소득세법 제89조 제1항 제3호, 시행령 제154조",
  highValueHouse: "소득세법 제95조 제3항, 시행령 제160조 제1항",
  longTermDeductionTable1: "소득세법 제95조 제2항 표 1, 시행령 제159조의3",
  longTermDeductionTable2: "소득세법 제95조 제2항 표 2, 시행령 제159조의4",
  heavyTaxation: "소득세법 제104조 제7항, 시행령 제167조의3·제167조의10·제167조의11",
});

// v0.3-B LAW_REFS (12종 — 신규 1키 추가):
const LAW_REFS = Object.freeze({
  incomeTaxAct: "소득세법 [법률 제21065호, 2026-01-02 시행]",
  incomeTaxEnforcement: "소득세법 시행령 [대통령령 제36129호, 2026-03-01 시행]",
  progressiveRate: "소득세법 제55조 제1항",
  transferTaxRate: "소득세법 제104조 제1항",
  basicDeduction: "소득세법 제103조",
  localIncomeTax: "지방세법 제103조의3",
  nonTaxation1Se1House: "소득세법 제89조 제1항 제3호, 시행령 제154조",
  highValueHouse: "소득세법 제95조 제3항, 시행령 제160조 제1항",
  longTermDeductionTable1: "소득세법 제95조 제2항 표 1, 시행령 제159조의3",
  longTermDeductionTable2: "소득세법 제95조 제2항 표 2, 시행령 제159조의4",
  heavyTaxation: "소득세법 제104조 제7항, 시행령 제167조의3·제167조의10·제167조의11",
  finalReturnAggregation: "소득세법 제104조 제5항(본문·1호·2호 본문·2호 단서) + 제55조 제1항", // ★ v0.3-B 신규
});
```

**키 추가 사유**:
- 의사결정 #13 — 동일 과세기간 다중 양도 시 1호·2호 채택 산식의 법령 인용 본문
- 본 키는 `progressiveRate`·`transferTaxRate`·`heavyTaxation`과 인용 영역이 다르므로 상호 배타적
- 인용 영역 분담: `progressiveRate` = 일반 누진세율 본문 / `transferTaxRate` = 양도세율표 본문 / `heavyTaxation` = 다주택 중과 가산세율 본문 / `finalReturnAggregation` = 동일 과세기간 다중 양도 시 1호·2호 채택 산식 본문

**시행령 인용 0건 영역** (사용자 39번째 짚음 정정):
- `finalReturnAggregation` 키 라벨 — 시행령 0건 인용 (법 제104조 ⑤ + 제55조 ① 본문만) ✅
- `heavyTaxation` 키 라벨 — 시행령 제167조의3·제167조의10·제167조의11 인용 (v0.3-A 본문 그대로 보존, 다주택 중과 자산 정의 영역) ✅

#### §3-2-3. 신규 노출 멤버 0종

본 패치는 신규 노출 멤버 0종이다. v0.3-B 산식 5단계 본문은 모두 호출 측 (`tax_engine.js` v0.3-B) 단일 책임 — 사용자 결정 옵션 (A) 채택.

#### §3-2-4. selfTest 0건 추가

본 패치는 `selfTest()` 검증 0건 추가한다. v0.3-A 7종 (`verifyProgressive` + `verifyLongTerm1Lookup` + `verifyLongTerm2HoldingLookup` + `verifyLongTerm2ResidenceLookup` + `verifyHeavyTaxRateAddition` + `verifyConstants` + `verifyLawRefs`) 모두 그대로 보존.

확정신고 v3 산식 검증은 engine 측 selfTest 책임 — `tax_engine.md` v0.3-B §9 단일 책임 (작업 창 #16 인계).

#### §3-2-5. 부트스트랩 가드 0건 추가

본 패치는 부트스트랩 가드 0건 추가한다. v0.3-A 가드 2종 (가드 1 + 가드 2 + 가드 2-A) 모두 그대로 보존.

### §3-3. 패치 라인 추정

| 영역 | 라인 |
|---|---|
| `RULE_VERSION` 1라인 갱신 | 1 라인 |
| `LAW_REFS.finalReturnAggregation` 1키 추가 | 2~3 라인 (객체 1행 + 주석) |
| **합계** | **+3~+4 라인** |

### §3-4. v0.3-A 회귀 안전성 영역 (검증 필수)

| 검증 항목 | v0.3-B 결과 |
|---|---|
| v0.3-A 26종 노출 멤버 시그니처·값 | 변경 0건 (`RULE_VERSION` 값 1건만 예외) |
| v0.3-A `LAW_REFS` 11종 키 | 변경 0건 (`finalReturnAggregation` 1키 추가만) |
| v0.3-A `selfTest` 7종 검증 | 모두 `ok === true` 그대로 통과 |
| v0.3-A `findHeavyTaxRateAddition` 클램프 정책 | 변경 0건 (`houseCount >= 3` 자동 클램프) |
| v0.3-A `Object.freeze` 정책 | 변경 0건 (미적용 — 모듈 스펙 §11-3) |

> **회귀 검증 영역**: 본 §3-4 표가 모두 ✅이면 v0.3-A 회귀 안전성 보장. 1건이라도 ❌ 시 즉시 롤백.

### §3-5. 본 작업지시서 §3 산출물

| 파일 | 산출 영역 |
|---|---|
| `js/tax_rules.js` | v0.3-A → v0.3-B 패치 (`RULE_VERSION` 1라인 + `LAW_REFS` 1키) |
| `tests/tax_rules.test.js` | `RULE_VERSION` strict-eq 1라인 갱신 (§5 별도) |

---

## §4. tax_engine.js v0.3-B 패치 (약 +250~+350 라인)

### §4-1. ENGINE_VERSION 갱신 (1라인)

```js
// v0.3-A:
const ENGINE_VERSION = "v0.3.0-A";

// v0.3-B (1라인 갱신):
const ENGINE_VERSION = "v0.3.0-B";
```

**갱신 사유**: 의사결정 #13 — 확정신고 v3 산식 5단계 본문 추가. v0.3-A의 13단계 단일 양도 파이프라인은 그대로 보존되며, 본 갱신은 v0.3-B 영역 식별만을 위한 1라인 갱신.

### §4-2. 누진 헬퍼 공개 노출 격상 (5/4 합의 결정 2번 영속화 영역)

v0.3-A에서 `findProgressiveTaxAmount`·`findHeavyProgressiveTaxAmount`는 단계 9 내부 함수로 동작했으나 IIFE 외부에 노출되지 않았다. v0.3-B에서 §5-7 산식 5단계가 본 함수를 호출해야 하므로 **공개 노출로 격상**한다.

> **시그니처 변동 0건**: 본 격상은 함수 본체 변경 없음. v0.3-A 회귀 영향 0건.

#### §4-2-1. findProgressiveTaxAmount 공개 노출

| 항목 | 내용 |
|---|---|
| 함수명 | `findProgressiveTaxAmount` |
| 입력 | `taxBase: number` (양도소득 과세표준, 정수 ≥ 0) |
| 출력 | `number` (제55조 ① 일반 누진세율 적용 산출세액, Math.floor 적용) |
| v0.3-A 위치 | 단계 9 내부 함수 (IIFE 내부 비공개) |
| v0.3-B 위치 | **IIFE 외부 공개 노출** — `window.TaxOpt.taxEngine.findProgressiveTaxAmount` |
| 호출 측 | §5-7 신규 — `calculateClause1AggregateProgressive` (2단계) + `calculateClause2PerTransferWithDanSeo` 단서 (3단계) |
| 의존 룰 | `taxRules.PROGRESSIVE_BRACKETS` + `taxRules.findBracket` |

**의사코드** (v0.3-A 단계 9 본문 그대로 — 변경 0건):

```js
function findProgressiveTaxAmount(taxBase) {
  // 입력 검증
  if (!Number.isInteger(taxBase) || taxBase < 0) {
    throw new Error(
      `findProgressiveTaxAmount: invalid taxBase=${taxBase} (정수 ≥ 0 필요)`
    );
  }
  
  // 누진 구간 룩업
  const bracket = taxRules.findBracket(taxBase);
  
  // 누적 baseTax + 초과액 × 한계세율
  const excessAmount = taxBase - bracket.threshold;
  const tax = bracket.baseTax + excessAmount * bracket.rate;
  
  return Math.floor(tax);
}
```

#### §4-2-2. findHeavyProgressiveTaxAmount 공개 노출 (5/4 합의 결정 2번)

| 항목 | 내용 |
|---|---|
| 함수명 | `findHeavyProgressiveTaxAmount` |
| 입력 | `taxBase: number`, `addition: number` (가산세율, 0.20 또는 0.30) |
| 출력 | `number` (제55조 ① + 제104조 ⑦ 가산세율 적용 산출세액, Math.floor 적용) |
| v0.3-A 위치 | 단계 9 내부 함수 (IIFE 내부 비공개) |
| v0.3-B 위치 | **IIFE 외부 공개 노출** — `window.TaxOpt.taxEngine.findHeavyProgressiveTaxAmount` |
| 호출 측 | §5-7 신규 — `calculateClause2PerTransferWithDanSeo` 단서 발동 시 (3단계) |
| 의존 룰 | `taxRules.PROGRESSIVE_BRACKETS` + `taxRules.findBracket` |

**의사코드** (v0.3-A 단계 9 본문 그대로 — 변경 0건, **5/4 합의 결정 2번 영역**):

```js
function findHeavyProgressiveTaxAmount(taxBase, addition) {
  // 입력 검증
  if (!Number.isInteger(taxBase) || taxBase < 0) {
    throw new Error(
      `findHeavyProgressiveTaxAmount: invalid taxBase=${taxBase} (정수 ≥ 0 필요)`
    );
  }
  if (typeof addition !== "number" || addition <= 0 || addition >= 1) {
    throw new Error(
      `findHeavyProgressiveTaxAmount: invalid addition=${addition} (0 < addition < 1 필요)`
    );
  }
  
  // 누진 구간 룩업
  const bracket = taxRules.findBracket(taxBase);
  
  // 누적 baseTax + 초과액 × (한계세율 + 가산세율)
  // ★ v0.3-A 누적 baseTax 산식 그대로 — Python 표준 누진공제 산식 미사용 (5/4 합의 결정 2번)
  const excessAmount = taxBase - bracket.threshold;
  const heavyRate = bracket.rate + addition;
  const tax = bracket.baseTax + excessAmount * heavyRate;
  
  return Math.floor(tax);
}
```

> **5/4 합의 결정 2번 영속화 영역**: 본 산식은 v0.3-A 단계 9 동적 재계산 본문 그대로. Python 표준 누진공제 산식 (한계세율 일괄 적용 후 누진공제 차감)은 미사용. 사유: v0.3-A 회귀 안전성 보장 — TC-011~014 정답값과 정합.

#### §4-2-3. IIFE 외부 노출 패턴

v0.3-A IIFE 패턴 (비공개 → 공개 격상):

```js
// v0.3-A 패턴:
window.TaxOpt = window.TaxOpt || {};
window.TaxOpt.taxEngine = (function() {
  // ... v0.3-A 21종 노출 멤버
  
  // 비공개 함수 (단계 9 내부)
  function findProgressiveTaxAmount(taxBase) { /* ... */ }
  function findHeavyProgressiveTaxAmount(taxBase, addition) { /* ... */ }
  
  return {
    ENGINE_VERSION,
    calculateSingleTransfer,
    isHeavyTaxationApplicable,
    // ... v0.3-A 21종
  };
})();

// v0.3-B 패턴 (공개 격상):
window.TaxOpt = window.TaxOpt || {};
window.TaxOpt.taxEngine = (function() {
  // ... v0.3-A 21종 노출 멤버 그대로
  
  // ★ v0.3-B 공개 노출 격상
  function findProgressiveTaxAmount(taxBase) { /* v0.3-A 본문 그대로 */ }
  function findHeavyProgressiveTaxAmount(taxBase, addition) { /* v0.3-A 본문 그대로 */ }
  
  // ★ v0.3-B 신규 §5-7 5단계 산식 본문 (§4-3 본문)
  function groupByTaxYear(perTransferResults) { /* §4-3-1 */ }
  function calculateClause1AggregateProgressive(perTransferResultsSameYear) { /* §4-3-2 */ }
  function getRateGroupKey(addition) { /* §4-3-3 */ }
  function calculateClause2PerTransferWithDanSeo(perTransferResultsSameYear) { /* §4-3-4 */ }
  function applyFinalReturnV3(perTransferResultsSameYear) { /* §4-3-5 */ }
  function distributeFinalTaxByShare(perTransferResultsSameYear, finalReturnResult) { /* §4-3-6 */ }
  
  return {
    ENGINE_VERSION, // "v0.3.0-B"
    calculateSingleTransfer,
    isHeavyTaxationApplicable,
    // ... v0.3-A 21종 그대로
    
    // ★ v0.3-B 신규 노출 7종 (공개 노출 2종 격상 + 5단계 산식 5종 + 헬퍼 1종 = 8종, 단 헬퍼는 비공개)
    findProgressiveTaxAmount,            // 공개 노출 격상
    findHeavyProgressiveTaxAmount,        // 공개 노출 격상
    groupByTaxYear,                       // §5-7 신규
    calculateClause1AggregateProgressive, // §5-7 신규
    calculateClause2PerTransferWithDanSeo,// §5-7 신규
    applyFinalReturnV3,                   // §5-7 신규
    distributeFinalTaxByShare,            // §5-7 신규
    // getRateGroupKey는 비공개 (calculateClause2 내부에서만 사용)
  };
})();
```

> **노출 멤버 합계**: v0.3-A 21종 + v0.3-B 신규 노출 7종 = **28종** (모듈 스펙 `tax_engine.md` v0.3-B §2 본문 23종과 차이 — 모듈 스펙은 5단계 산식 5종을 외부 호출 측 (`scenario_engine.js`)이 단일 진입하는 것으로 가정. Claude Code는 모듈 스펙 §2 본문을 정독 후 노출 정책 결정).

> **노출 정책 결정 영역**: 본 §4-2-3 패턴은 5단계 산식 5종을 모두 공개 노출하는 안. 모듈 스펙 §2는 23종 (공개 노출 2종 + 5단계 산식 5종 = 7종 중 일부 비공개) 가능성 시사. **Claude Code는 모듈 스펙 §2 본문을 정독 후 결정** — 5단계 산식 5종 모두 공개 노출 (28종) vs 일부 비공개 (23~28종 중 결정).

### §4-3. §5-7 신규 5단계 산식 본문 (★ 핵심 — 약 +200 라인)

본 §4-3은 의사결정 #13 본문 영역. 5단계 산식은 명세서 v0.3-B §5-7-3 + 모듈 스펙 `tax_engine.md` v0.3-B §5-7-3 본문 단일 진본 인용 그대로.

#### §4-3-1. 1단계 — groupByTaxYear

| 항목 | 내용 |
|---|---|
| 함수명 | `groupByTaxYear` |
| 입력 | `perTransferResults: TaxResult[]` (시뮬레이션 후 양도별 결과 배열, 길이 ≥ 1) |
| 출력 | `Map<number, TaxResult[]>` (연도별 그룹화 결과, 키 = 양도일 연도, 값 = 동일 연도 양도 결과 배열) |
| 부수효과 | 없음 |
| 결정성 | 동일 입력 → 동일 출력 |
| 호출 측 | `simulateScenarioWithStateTransition` (`scenario_engine.js` 측, 작업 창 #17+ 인계) |
| 의존 함수 | 없음 |

**의사코드** (명세서 §5-7-3 1단계 본문 그대로):

```js
function groupByTaxYear(perTransferResults) {
  if (!Array.isArray(perTransferResults)) {
    throw new Error(
      `groupByTaxYear: perTransferResults must be Array, got ${typeof perTransferResults}`
    );
  }
  
  const groups = new Map();
  for (const r of perTransferResults) {
    if (!Number.isInteger(r.saleYear)) {
      throw new Error(
        `groupByTaxYear: r.saleYear must be integer, got ${r.saleYear}`
      );
    }
    
    const year = r.saleYear; // 양도일 속하는 과세연도
    if (!groups.has(year)) groups.set(year, []);
    groups.get(year).push(r);
  }
  
  return groups;
}
```

> **`saleYear` 산출 영역**: 양도일(`saleDate`)의 연도 부분 (예: "2026-09-15" → 2026). 단계 0 (validateCaseData) 또는 단계 1 (transferGain 산출 직전)에서 `result.steps.saleYear` 채움 (§4-4 본문).

#### §4-3-2. 2단계 — calculateClause1AggregateProgressive (1호 합산 누진)

| 항목 | 내용 |
|---|---|
| 함수명 | `calculateClause1AggregateProgressive` |
| 입력 | `perTransferResultsSameYear: TaxResult[]` (동일 과세연도 양도 결과 배열, 길이 ≥ 1) |
| 출력 | `number` (1호 산출세액 — 모든 자산 과세표준 합산 × 제55조 ① 일반 누진세율 1회 적용) |
| 부수효과 | 없음 |
| 결정성 | 동일 입력 → 동일 출력 |
| 호출 측 | `applyFinalReturnV3` (4단계) |
| 의존 함수 | `findProgressiveTaxAmount` (§4-2-1 공개 노출 격상 영역) |

**의사코드** (명세서 §5-7-3 2단계 본문 그대로):

```js
function calculateClause1AggregateProgressive(perTransferResultsSameYear) {
  if (!Array.isArray(perTransferResultsSameYear) || perTransferResultsSameYear.length === 0) {
    throw new Error(
      `calculateClause1AggregateProgressive: perTransferResultsSameYear must be non-empty Array`
    );
  }
  
  // 모든 자산 과세표준 합산 (중과 자산 포함 — 사용자 39번째 짚음 정정 영역)
  const totalTaxBase = perTransferResultsSameYear.reduce(
    (sum, r) => sum + r.taxBase, 0
  );
  
  // 제55조 ① 일반 누진세율 1회 적용 (가산세율 미적용)
  return findProgressiveTaxAmount(totalTaxBase);
}
```

> **사용자 39번째 짚음 정정 영역**: v2 잘못 정정안 → "1호 합산을 일반과세 자산만으로 좁게 적용". v3 정확본 → **모든 자산 과세표준 합산** (중과 자산 포함). 사유: 법 제104조 ⑤ 1호 본문은 "해당 과세기간의 양도소득과세표준 합계액"으로 명시 — 모든 자산 합산.

> **가산세율 미적용 사유**: 1호는 "제55조제1항에 따른 세율"만 인용 — 제104조 ⑦ (다주택 중과)는 1호 영역 미적용. 일반 누진세율 1회 적용.

#### §4-3-3. getRateGroupKey 헬퍼 (5/4 합의 결정 1번 영역 — B-022 부동소수점 회피)

| 항목 | 내용 |
|---|---|
| 함수명 | `getRateGroupKey` |
| 입력 | `addition: number` (가산세율, 0.0 또는 0.20 또는 0.30) |
| 출력 | `number` (정수 키, 0 또는 20 또는 30) |
| 부수효과 | 없음 |
| 결정성 | 동일 입력 → 동일 출력 |
| 호출 측 | `calculateClause2PerTransferWithDanSeo` (3단계) — Map 키로 사용 |
| 노출 정책 | 비공개 (IIFE 내부) — 외부 호출 불요 |

**의사코드** (5/4 합의 결정 1번 본문 그대로):

```js
function getRateGroupKey(addition) {
  // ★ 부동소수점 회피용 정수 키 (B-022 — 5/4 합의 결정 1번)
  // 0.0 → 0, 0.20 → 20, 0.30 → 30
  return Math.round(addition * 100);
}
```

> **5/4 합의 결정 1번 영속화 영역**: B-022 (양도소득세 정수 처리 — v0.3-B 무영향) 부동소수점 회피용 정수 키 헬퍼. v0.3-B 산식 5단계 영역에서만 사용. v0.3-A 회귀 영역은 영향 0건.

> **⚠️ 모듈 스펙 본문 정정 인계 영역 (영속화 차단 영역 — 의사결정 #14 등록 권고)**: 모듈 스펙 `tax_engine.md` v0.3-B §5-7-3 line 599~602 본문은 `getRateGroupKey` 반환 형식이 **문자열 반환**으로 영속화되어 있다. 본 작업지시서 §4-3-3 본문은 **정수 반환** (`Math.round(addition * 100)`) 채택 — 5/4 합의 결정 1번 "정수 키 채택" 본문 정합 + R-B-6 회귀 그룹 정수 비교 (`assert.strictEqual(taxEngine.getRateGroupKey(0.20), 20)`) 정합. 본 정정 인계 영역은 후속 처리 권고: (a) 작업지시서 영속화 후 작업 창 #15 후속 보강으로 모듈 스펙 line 599~602 정수 반환으로 정정 / (b) post-MVP 보강. **Claude Code는 본 작업지시서 §4-3-3 본문 (정수 반환) 단일 진본 채택** — 모듈 스펙 본문과 충돌 시 본 작업지시서 우선.

#### §4-3-4. 3단계 — calculateClause2PerTransferWithDanSeo (2호 단독 합계 + 단서)

| 항목 | 내용 |
|---|---|
| 함수명 | `calculateClause2PerTransferWithDanSeo` |
| 입력 | `perTransferResultsSameYear: TaxResult[]` (동일 과세연도 양도 결과 배열, 길이 ≥ 1) |
| 출력 | `number` (2호 산출세액 — 자산별 단독 합계 + 단서 발동 시 동일 호 세율 자산 합산) |
| 부수효과 | 없음 |
| 결정성 | 동일 입력 → 동일 출력 |
| 호출 측 | `applyFinalReturnV3` (4단계) |
| 의존 함수 | `findProgressiveTaxAmount` (1호 단서 발동 시) + `findHeavyProgressiveTaxAmount` (중과 단서 발동 시) + `getRateGroupKey` (정수 키 헬퍼) |

**의사코드** (명세서 §5-7-3 3단계 본문 그대로):

```js
function calculateClause2PerTransferWithDanSeo(perTransferResultsSameYear) {
  if (!Array.isArray(perTransferResultsSameYear) || perTransferResultsSameYear.length === 0) {
    throw new Error(
      `calculateClause2PerTransferWithDanSeo: perTransferResultsSameYear must be non-empty Array`
    );
  }
  
  // 그룹화: heavyRateAddition 별 (0.0=일반, 0.20=2주택, 0.30=3주택)
  // 부동소수점 회피용 정수 키 (5/4 합의 결정 1번)
  const byHeavyAddition = new Map();
  for (const r of perTransferResultsSameYear) {
    const addition = r.heavyRateAddition !== null ? r.heavyRateAddition : 0.0;
    const key = getRateGroupKey(addition); // 정수 키 — Math.round 패턴
    if (!byHeavyAddition.has(key)) {
      byHeavyAddition.set(key, { addition, group: [] });
    }
    byHeavyAddition.get(key).group.push(r);
  }
  
  let total = 0;
  for (const [key, { addition, group }] of byHeavyAddition.entries()) {
    if (group.length >= 2) {
      // 단서 발동: 동일 호 세율 자산 ≥ 2 → 합산 후 호별 세율
      const sumBase = group.reduce((s, r) => s + r.taxBase, 0);
      const aggregatedInGroup = (addition === 0.0)
        ? findProgressiveTaxAmount(sumBase)             // 일반 누진세율
        : findHeavyProgressiveTaxAmount(sumBase, addition); // 가산세율 적용 누진세율
      const sumSolo = group.reduce((s, r) => s + r.calculatedTax, 0);
      
      // MAX(합산 결과, 단독 합계) 그룹별 채택
      total += Math.max(aggregatedInGroup, sumSolo);
    } else {
      // 단일 자산: 단독 그대로
      total += group[0].calculatedTax;
    }
  }
  
  return total;
}
```

> **단서 발동 조건**: 동일 호 세율 자산 ≥ 2건 (그룹 크기 ≥ 2). 본 케이스는 다음 영역에서 발동:
> - 2주택 중과 자산 ≥ 2건 (`heavyRateAddition === 0.20` 그룹) → `findHeavyProgressiveTaxAmount(sumBase, 0.20)` 호출
> - 3주택 중과 자산 ≥ 2건 (`heavyRateAddition === 0.30` 그룹) → `findHeavyProgressiveTaxAmount(sumBase, 0.30)` 호출
> - 일반과세 자산 ≥ 2건 (`heavyRateAddition === 0.0` 그룹) → `findProgressiveTaxAmount(sumBase)` 호출

> **MAX(합산, 단독) 채택**: 법 제104조 ⑤ 2호 단서 본문 — "각 자산의 양도소득과세표준을 합산한 것에 대하여 ... 산출한 세액 중에서 큰 산출세액의 합계액". 그룹별 합산 결과와 단독 합계 중 큰 것 채택.

#### §4-3-5. 4단계 — applyFinalReturnV3 (MAX(1호, 2호) 채택)

| 항목 | 내용 |
|---|---|
| 함수명 | `applyFinalReturnV3` |
| 입력 | `perTransferResultsSameYear: TaxResult[]` (동일 과세연도 양도 결과 배열, 길이 ≥ 0) |
| 출력 | `{ finalReturnTax, method, diff, aggregateTaxClause1?, aggregateTaxClause2?, perTransferTax? }` |
| 부수효과 | 없음 |
| 결정성 | 동일 입력 → 동일 출력 |
| 호출 측 | `simulateScenarioWithStateTransition` (`scenario_engine.js` 측, 작업 창 #17+ 인계) |
| 의존 함수 | `calculateClause1AggregateProgressive` + `calculateClause2PerTransferWithDanSeo` |

**의사코드** (명세서 §5-7-3 4단계 본문 그대로 — **5/4 합의 결정 3번 영역**):

```js
function applyFinalReturnV3(perTransferResultsSameYear) {
  if (!Array.isArray(perTransferResultsSameYear)) {
    throw new Error(
      `applyFinalReturnV3: perTransferResultsSameYear must be Array`
    );
  }
  
  // 빈 배열 분기
  if (perTransferResultsSameYear.length === 0) {
    return { finalReturnTax: 0, method: "NONE", diff: 0 };
  }
  
  // ★ 5/4 합의 결정 3번 — 단일 양도 분기 (TC-001~014 회귀 보존 영역)
  if (perTransferResultsSameYear.length === 1) {
    const r = perTransferResultsSameYear[0];
    return {
      finalReturnTax: r.calculatedTax,
      method: "SINGLE_TRANSFER",
      diff: 0,
    };
  }
  
  // 다중 양도 — 1호·2호 산출
  const clause1Tax = calculateClause1AggregateProgressive(perTransferResultsSameYear);
  const clause2Tax = calculateClause2PerTransferWithDanSeo(perTransferResultsSameYear);
  
  // 자산별 단독 합계 (예정신고 비교 영역)
  const perTransferTotal = perTransferResultsSameYear.reduce(
    (s, r) => s + r.calculatedTax, 0
  );
  
  // MAX 채택 (법 제104조 ⑤ 본문 — "다음 각 호의 금액 중 큰 것")
  const finalReturnTax = Math.max(clause1Tax, clause2Tax);
  const method = (finalReturnTax === clause1Tax)
    ? "CLAUSE_1_AGGREGATE_PROGRESSIVE"
    : "CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO";
  
  return {
    finalReturnTax,
    method,
    diff: finalReturnTax - perTransferTotal,
    aggregateTaxClause1: clause1Tax,
    aggregateTaxClause2: clause2Tax,
    perTransferTax: perTransferTotal,
  };
}
```

> **5/4 합의 결정 3번 영속화 영역 (★ 회귀 안전성 핵심)**: `length === 1` 분기는 TC-001~014 14건 단일 양도 입력 시 진입. `finalReturnTax = r.calculatedTax` 그대로 반환 → totalTax 100% 일치. 의사결정 #13 회귀 안전성 영역 정본.

> **MAX 채택 영역**: xlsx 시트 19 (산식정정_v3) 본문 직접 인용 — TC-S05·S06·S07 모든 시나리오 method = `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` (2호 채택). 사유: 다주택 중과 적용 자산 1건 이상 시 2호 산출세액이 1호보다 항상 큼. 1호 채택 발동 케이스 (모든 자산 일반과세 + 양도차익 작은 케이스)는 검증팀 정확 검증 영역.

#### §4-3-6. 5단계 — distributeFinalTaxByShare (양도별 비례 분배)

| 항목 | 내용 |
|---|---|
| 함수명 | `distributeFinalTaxByShare` |
| 입력 | `perTransferResultsSameYear: TaxResult[]`, `finalReturnResult: { finalReturnTax, method, ... }` |
| 출력 | (부수효과로 `perTransferResultsSameYear[i].finalCalculatedTax` 채움, 명시 반환값 없음) |
| 부수효과 | **있음** — `result.steps.finalCalculatedTax` 필드 mutation (§7 불변성 약속 v0.3-B 예외 영역) |
| 결정성 | 동일 입력 → 동일 출력 |
| 호출 측 | `simulateScenarioWithStateTransition` (`applyFinalReturnV3` 호출 직후) |
| 분배 산식 | 양도별 `calculatedTax / Σ calculatedTax` 비율 × `finalReturnTax` (Math.floor 적용) |

**의사코드** (명세서 §5-7-3 5단계 본문 그대로):

```js
function distributeFinalTaxByShare(perTransferResultsSameYear, finalReturnResult) {
  if (!Array.isArray(perTransferResultsSameYear)) {
    throw new Error(
      `distributeFinalTaxByShare: perTransferResultsSameYear must be Array`
    );
  }
  if (!finalReturnResult || typeof finalReturnResult.finalReturnTax !== "number") {
    throw new Error(
      `distributeFinalTaxByShare: invalid finalReturnResult`
    );
  }
  
  // 단일 양도 분기 — finalCalculatedTax = calculatedTax 그대로 (회귀 안전성)
  if (finalReturnResult.method === "SINGLE_TRANSFER") {
    perTransferResultsSameYear[0].finalCalculatedTax = 
      finalReturnResult.finalReturnTax;
    return;
  }
  
  // 다중 양도 — 비례 분배
  const totalCalc = perTransferResultsSameYear.reduce(
    (s, r) => s + r.calculatedTax, 0
  );
  
  // 전체 비과세 케이스 — 분배 0
  if (totalCalc === 0) {
    for (const r of perTransferResultsSameYear) {
      r.finalCalculatedTax = 0;
    }
    return;
  }
  
  // 양도별 비례 분배 (Math.floor 적용 — B-022 절사 정책 정합)
  for (const r of perTransferResultsSameYear) {
    const share = r.calculatedTax / totalCalc;
    r.finalCalculatedTax = Math.floor(finalReturnResult.finalReturnTax * share);
  }
}
```

> **부수효과 영역**: 본 함수는 `result.steps.finalCalculatedTax`를 mutation한다. 본 모듈 §7 불변성 약속 (v0.2.1 + v0.3-A) 영역의 예외 — v0.3-B 산식 5단계 처리 후에만 1회 mutation 허용. 호출 측 (`scenario_engine.js`)은 본 mutation을 인지한 후 `localIncomeTax`·`totalTax`·`netAfterTaxSaleAmount`·`effectiveTaxRate` 재산출.

> **반올림 vs 절사**: `Math.floor` 적용 — v0.3-A 단계 9·10·11의 절사 정책과 정합 (B-022 정수 처리 영역).

> **분배 후 합계 vs 원본 합계**: 분배 후 `Σ finalCalculatedTax`는 `Math.floor` 누적 손실로 `finalReturnTax`보다 약간 작을 수 있음 (최대 N−1원, N = 양도 개수). xlsx 시트 19 검증 결과 — TC-S06 SC-3에서 **−1원 차이** 발생 (894,816,998 → 894,816,997). 본 차이는 v3 산식 본문의 정합 영역 (Math.floor 정책 그대로). 호출 측은 본 차이를 issueFlag로 표시 미고려 (검증 영역).

### §4-4. result.steps 신규 4종 (모듈 스펙 §4-2-2-B 본문 그대로)

v0.3-A `result.steps` 27종 → v0.3-B 31종 (4종 추가).

| 필드 | 타입 | 단계 | 의미 |
|---|---|---|---|
| **`saleYear`** | `number` | 양도일 산출 직후 (단계 0 또는 1) | 양도일이 속하는 과세연도 (양도일의 연도 부분). §4-3-1 `groupByTaxYear`에서 그룹화 키로 사용 |
| **`finalCalculatedTax`** | `number` | §5-7 산식 5단계 처리 후 | 확정신고 v3 산식 적용 후 양도별 산출세액 (Math.floor 적용). 단일 양도 (length === 1) 시 = `calculatedTax` 그대로. 다중 양도 시 = MAX(1호, 2호) × (calculatedTax / Σ calculatedTax) 비례 분배 |
| **`finalReturnMethod`** | `string` | §4-3-5 `applyFinalReturnV3` 결과 | 확정신고 산식 채택 영역 식별: `"SINGLE_TRANSFER"` / `"CLAUSE_1_AGGREGATE_PROGRESSIVE"` / `"CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO"` |
| **`finalReturnDiff`** | `number` | §4-3-5 결과 | 확정신고 영역 차이 (`finalReturnTax − Σ calculatedTax`). 단일 양도 시 0. 1호 채택 시 양수 가능. 2호 채택 + 단서 발동 시 양수 가능 |

#### §4-4-1. 단일 양도 입력 시 (TC-001~014 회귀 영역)

| 필드 | 값 |
|---|---|
| `saleYear` | 양도일의 연도 (정상 채움) |
| `finalCalculatedTax` | `calculatedTax` 그대로 (`length === 1` → `SINGLE_TRANSFER` 분기) |
| `finalReturnMethod` | `"SINGLE_TRANSFER"` |
| `finalReturnDiff` | `0` |

> **회귀 안전성**: TC-001~014 14건 단일 양도 입력 시 v0.3-B 신규 4종 필드는 위 표대로 채워진다. v0.3-A 결과 객체와 비교 시 v0.3-B 신규 4종 필드를 무시하면 100% 동일. `localIncomeTax`·`totalTax`·`netAfterTaxSaleAmount`·`effectiveTaxRate`는 `finalCalculatedTax = calculatedTax`이므로 v0.3-A 그대로.

#### §4-4-2. terminateAt2 === true 시 (1세대1주택 비과세 케이스)

| 필드 | 값 | 사유 |
|---|---|---|
| `saleYear` | 양도일의 연도 (정상 채움) | 단계 2 종료와 무관하게 양도일은 입력 영역. 정상 채움 |
| `finalCalculatedTax` | `0` | 비과세 케이스 → calculatedTax = 0 → 단일 양도 분기 → finalCalculatedTax = 0 |
| `finalReturnMethod` | `"SINGLE_TRANSFER"` | 단일 양도 입력 (terminateAt2=true 케이스는 비과세 처리이므로 항상 단일 시나리오) |
| `finalReturnDiff` | `0` | 단일 양도 분기 → diff = 0 |

#### §4-4-3. 다중 양도 입력 시 (TC-S05·S06·S07 신규 영역)

| 필드 | 값 |
|---|---|
| `saleYear` | 양도일의 연도 (각 양도별) |
| `finalCalculatedTax` | `Math.floor(finalReturnTax × calculatedTax / Σ calculatedTax)` |
| `finalReturnMethod` | `"CLAUSE_1_AGGREGATE_PROGRESSIVE"` 또는 `"CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO"` (xlsx 시트 19 검증 결과 모두 후자 채택) |
| `finalReturnDiff` | `finalReturnTax − Σ calculatedTax` (양수 가능) |

### §4-5. issueFlag 신규 2종 (모듈 스펙 §6-B 본문 그대로)

v0.3-A issueFlag 카탈로그 25종 → v0.3-B 27종 (2종 추가, 발동 조건 상호 배타).

#### §4-5-1. FINAL_RETURN_AGGREGATE_PROGRESSIVE_APPLIED (info)

| 항목 | 내용 |
|---|---|
| code | `FINAL_RETURN_AGGREGATE_PROGRESSIVE_APPLIED` |
| severity | `info` |
| 발동 조건 | `applyFinalReturnV3` 결과 `method === "CLAUSE_1_AGGREGATE_PROGRESSIVE"` (1호 채택 — 합산 누진세율이 자산별 단독 합계보다 큼) |
| 메시지 | "확정신고 시 1호 합산 누진세율 적용 (법 제104조 ⑤ 1호) — Σ 과세표준에 일반 누진세율 1회 적용" |
| 법령 인용 | `LAW_REFS.finalReturnAggregation` ("소득세법 제104조 제5항(본문·1호·2호 본문·2호 단서) + 제55조 제1항") |
| 발동 빈도 | 모든 자산 일반과세 + 양도차익 작은 케이스 (TC-S05·S06·S07에서 발동 0건 — 검증팀 정확 검증 영역) |

#### §4-5-2. FINAL_RETURN_DAN_SEO_APPLIED (info)

| 항목 | 내용 |
|---|---|
| code | `FINAL_RETURN_DAN_SEO_APPLIED` |
| severity | `info` |
| 발동 조건 | `applyFinalReturnV3` 결과 `method === "CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO"` (2호 채택 — 자산별 단독 합계 + 단서 발동) |
| 메시지 | "확정신고 시 2호 단서 적용 (법 제104조 ⑤ 2호 단서) — 동일 호 세율 자산 ≥ 2건 합산 후 호별 세율 + MAX(합산, 단독) 채택" |
| 법령 인용 | `LAW_REFS.finalReturnAggregation` |
| 발동 빈도 | TC-S05 4건 + TC-S06 6건 + TC-S07 SC-4 (분산 미적용 시) 1건 = 총 11건 (xlsx 시트 19 검증 영역) |

#### §4-5-3. 발동 조건 상호 배타

본 2종 issueFlag는 발동 조건이 **상호 배타적**:
- `length === 1` (단일 양도) → method === `"SINGLE_TRANSFER"` → 본 2종 모두 미발동
- `length >= 2` + 1호 채택 → `FINAL_RETURN_AGGREGATE_PROGRESSIVE_APPLIED` 발동
- `length >= 2` + 2호 채택 → `FINAL_RETURN_DAN_SEO_APPLIED` 발동

### §4-6. collectIssueFlags 본문 보강 (5/4 합의 결정 4번 영역)

v0.3-A `collectIssueFlags` 함수 본체에 v0.3-B 신규 issueFlag 2종 발동 조건 추가.

**의사코드** (v0.3-A 본문 + v0.3-B 신규 분기 추가):

```js
function collectIssueFlags(caseData, intermediates, result) {
  const flags = [];
  
  // ... v0.3-A 25종 issueFlag 발동 조건 본문 (변경 0건)
  
  // ★ v0.3-B 신규 — 확정신고 v3 산식 적용 결과 issueFlag (발동 조건 상호 배타)
  if (result.steps.finalReturnMethod === "CLAUSE_1_AGGREGATE_PROGRESSIVE") {
    flags.push({
      code: "FINAL_RETURN_AGGREGATE_PROGRESSIVE_APPLIED",
      severity: "info",
      message: "확정신고 시 1호 합산 누진세율 적용 (법 제104조 ⑤ 1호) — Σ 과세표준에 일반 누진세율 1회 적용",
      lawRef: taxRules.LAW_REFS.finalReturnAggregation,
    });
  } else if (result.steps.finalReturnMethod === "CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO") {
    flags.push({
      code: "FINAL_RETURN_DAN_SEO_APPLIED",
      severity: "info",
      message: "확정신고 시 2호 단서 적용 (법 제104조 ⑤ 2호 단서) — 동일 호 세율 자산 ≥ 2건 합산 후 호별 세율 + MAX(합산, 단독) 채택",
      lawRef: taxRules.LAW_REFS.finalReturnAggregation,
    });
  }
  // method === "SINGLE_TRANSFER" → 본 2종 모두 미발동 (TC-001~014 회귀 영역)
  
  return flags;
}
```

> **호출 위치**: `collectIssueFlags`는 `calculateSingleTransfer` 단계 13 (issueFlag 수집) 단계에서 호출. 단일 양도 시 `result.steps.finalReturnMethod === "SINGLE_TRANSFER"`이므로 v0.3-B 신규 2종 미발동 — TC-001~014 회귀 영역.

> **다중 양도 시 호출 영역**: `scenario_engine.js` 측 `simulateScenarioWithStateTransition`에서 `applyFinalReturnV3` + `distributeFinalTaxByShare` 호출 후, 각 양도별 `result.steps.finalReturnMethod` 채움. 본 채움 후 `collectIssueFlags` 재호출하여 v0.3-B 신규 2종 발동 — 호출 측 책임 영역.

### §4-7. 패치 라인 추정

| 영역 | 라인 |
|---|---|
| `ENGINE_VERSION` 1라인 갱신 | 1 라인 |
| `findProgressiveTaxAmount` 공개 노출 격상 (return 객체 1라인 추가) | 1 라인 |
| `findHeavyProgressiveTaxAmount` 공개 노출 격상 (return 객체 1라인 추가) | 1 라인 |
| `groupByTaxYear` 신규 (1단계) | ~15 라인 |
| `calculateClause1AggregateProgressive` 신규 (2단계) | ~10 라인 |
| `getRateGroupKey` 신규 (헬퍼) | ~5 라인 |
| `calculateClause2PerTransferWithDanSeo` 신규 (3단계) | ~35 라인 |
| `applyFinalReturnV3` 신규 (4단계) | ~40 라인 |
| `distributeFinalTaxByShare` 신규 (5단계) | ~30 라인 |
| `result.steps` 신규 4종 채움 본문 (`saleYear` 단계 0/1 영역 + `finalCalculatedTax`·`finalReturnMethod`·`finalReturnDiff` 단계 12 영역) | ~30 라인 |
| `collectIssueFlags` 본문 보강 (v0.3-B 신규 2종 분기) | ~15 라인 |
| return 객체 v0.3-B 신규 노출 멤버 추가 (5단계 산식 5종 + 헬퍼 2종) | ~7 라인 |
| 주석·블록 주석·법령 인용 본문 | ~50~150 라인 |
| **합계** | **+240~+340 라인** (시스템 프롬프트 §7-2 추정 +250~+350 라인 정합) |

### §4-8. v0.3-A 회귀 안전성 영역 (검증 필수)

| 검증 항목 | v0.3-B 결과 |
|---|---|
| TC-001~014 14건 totalTax | 100% 일치 (단일 양도 → `length === 1` → `SINGLE_TRANSFER` 분기 → calculatedTax 그대로) |
| v0.3-A 21종 노출 멤버 시그니처·반환 형식 | 변경 0건 (`ENGINE_VERSION` 1라인만 예외) |
| v0.3-A `result.steps` 27종 | 그대로 보존 (v0.3-B 신규 4종 추가) |
| v0.3-A issueFlag 25종 | 그대로 보존 (v0.3-B 신규 2종 추가, 발동 조건 상호 배타) |
| v0.3-A 단계 4·9 본문 (다주택 중과 분기) | 변경 0건 — 양도 1건당 calculatedTax 산출은 v0.3-A 그대로 |
| 13단계 단일 양도 파이프라인 | 본문 변경 0건 |
| `findHeavyTaxRateAddition` 클램프 정책 | 변경 0건 |

> **회귀 깨지면 즉시 롤백**: 본 §4-8 표가 모두 ✅이면 v0.3-A 회귀 안전성 보장. 1건이라도 ❌ 시 즉시 롤백.

### §4-9. 본 작업지시서 §4 산출물

| 파일 | 산출 영역 |
|---|---|
| `js/tax_engine.js` | v0.3-A → v0.3-B 패치 (`ENGINE_VERSION` + 누진 헬퍼 공개 노출 + §5-7 5단계 산식 + `result.steps` 신규 4종 + issueFlag 신규 2종) |
| `tests/tax_engine.test.js` | `ENGINE_VERSION` strict-eq 1라인 갱신 + R-B 회귀 그룹 (§6 별도) |

---

## §5. tests/tax_rules.test.js v0.3-B 패치 (1라인 갱신)

### §5-1. 변경 정책 + 예외 단서

**원칙**: v0.3-A 회귀 N건 (v0.1·v0.2 baseline 150 + v0.3-A 신규 N건)은 그대로 보존한다. 케이스 삭제·시그니처 변경 금지.

**예외 단서 (1라인 갱신 허용)**:
- `RULE_VERSION` strict-eq 비교 라인이 있으면 `'v0.3.1-final-return-v3'`로 1라인 갱신. 케이스 삭제 금지.
- `RULE_VERSION` 패턴 매칭 라인 (예: `/^v0\./`)이라면 갱신 불요.

### §5-2. 갱신 본문

```js
// v0.3-A:
assert.strictEqual(taxRules.RULE_VERSION, 'v0.3.0-post-20260510');

// v0.3-B (1라인 갱신):
assert.strictEqual(taxRules.RULE_VERSION, 'v0.3.1-final-return-v3');
```

### §5-3. v0.3-B 신규 회귀 그룹 R-A (LAW_REFS.finalReturnAggregation 검증)

v0.3-B 신규 1키 추가 검증을 위해 다음 회귀 그룹 추가 (선택적):

```js
describe('R-A: LAW_REFS.finalReturnAggregation (v0.3-B 신규)', () => {
  it('LAW_REFS에 finalReturnAggregation 키가 존재', () => {
    assert.strictEqual(
      typeof taxRules.LAW_REFS.finalReturnAggregation,
      'string'
    );
  });
  
  it('finalReturnAggregation 값이 정확본 인용', () => {
    assert.strictEqual(
      taxRules.LAW_REFS.finalReturnAggregation,
      "소득세법 제104조 제5항(본문·1호·2호 본문·2호 단서) + 제55조 제1항"
    );
  });
  
  it('finalReturnAggregation 값에 시행령 인용 0건', () => {
    // 사용자 39번째 짚음 정정 영역 — 시행령 인용 폐기 검증
    assert.strictEqual(
      taxRules.LAW_REFS.finalReturnAggregation.includes("시행령"),
      false
    );
    assert.strictEqual(
      taxRules.LAW_REFS.finalReturnAggregation.includes("제167조"),
      false
    );
  });
});
```

### §5-4. 회귀 통과 기준

| 항목 | 기준 |
|---|---|
| v0.1·v0.2 baseline | 150건 모두 통과 |
| v0.3-A 신규 그룹 (TC-011~014 회귀 + sanity) | 모두 통과 |
| v0.3-B 신규 그룹 R-A | 3건 모두 통과 (선택적) |

---

## §6. tests/tax_engine.test.js v0.3-B 패치

### §6-1. 변경 정책 + 예외 단서

**원칙**: v0.3-A 회귀 N건 (v0.1·v0.2 baseline 534 + v0.3-A 신규 N건 + TC-011~014)은 그대로 보존한다. 케이스 삭제·시그니처 변경 금지.

**예외 단서 (1라인 갱신 허용)**:
- `ENGINE_VERSION` strict-eq 비교 라인이 있으면 `'v0.3.0-B'`로 1라인 갱신.

### §6-2. v0.3-A 회귀 그룹 R-A (TC-001~014 14건 — 단일 양도 회귀)

본 회귀 그룹은 **v0.3-A 그대로 보존** (변경 0건). 단일 양도 입력 시 v0.3-B `applyFinalReturnV3`의 `length === 1` 분기 → `SINGLE_TRANSFER` → `finalCalculatedTax = calculatedTax` 그대로 → totalTax 100% 일치 검증.

```js
// v0.3-A:
assert.strictEqual(taxEngine.ENGINE_VERSION, 'v0.3.0-A');

// v0.3-B (1라인 갱신):
assert.strictEqual(taxEngine.ENGINE_VERSION, 'v0.3.0-B');
```

> **TC-001~014 14건 totalTax 정답값**: v0.3-A 골든셋 그대로. 본 작업지시서는 본 정답값을 직접 인용하지 않음 (v0.3-A 작업지시서 `06_tax_engine_v0_3.md` §11 본문 단일 진본).

### §6-3. v0.3-B 신규 회귀 그룹 R-B (TC-S05·S06·S07 v3 — 다중 양도 신규)

본 회귀 그룹은 **v0.3-B 신규** — 다중 양도 시 확정신고 v3 산식 5단계 적용 결과 검증.

#### §6-3-1. 정답값 영속화 (xlsx 시트 19 본문 직접 인용)

| TC | 시나리오 | v3 Σ totalTax | method |
|---|---|---|---|
| TC-S05 | SC-1 (A→C) | 606,694,000 | `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` |
| TC-S05 | SC-2 (C→A) | 521,911,500 | `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` |
| TC-S05 | SC-3 (B→C) | 296,438,998 | `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` |
| TC-S05 | **SC-4 (C→B)** | **255,051,500** ★ rank 1 | `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` |
| TC-S06 | SC-1 (A→B→C) | 960,212,000 | `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` |
| TC-S06 | SC-3 (B→A→C) | 894,816,997 | `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` (Math.floor 누적 손실 −1원) |
| TC-S06 | **SC-6 (C→B→A)** | **296,453,076** ★ rank 1 | `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` |
| TC-S07 | **SC-6 (C→B 분산 [2026, 2027])** | **253,401,500** ★ rank 1 | **각 연도별 `SINGLE_TRANSFER`** ※ |
| TC-S07 | SC-4 (C→B 동일 연도) | 255,051,500 | `CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO` |

> **※ TC-S07 SC-6 method 표기**: 분산 양도 [2026: SC-6 1건, 2027: SC-6 1건] — 각 연도별 `groupByTaxYear` 적용 후 length === 1 분기 → 양도별 `finalReturnMethod = "SINGLE_TRANSFER"`. 본 회귀 그룹 R-B 본문에서 양도별 method 검증.

> **TC-S07 절세 영역 검증**: SC-6 분산 (253,401,500) vs SC-4 동일 연도 (255,051,500) — **1,650,000원 절세** (양도소득 기본공제 250만원 × 2 연도 효과).

#### §6-3-2. R-B 회귀 그룹 본문 (의사코드)

본 회귀 그룹은 시나리오 엔진 (`scenario_engine.js`, 작업 창 #17+ 인계) 의존. 본 작업 창 #16 산출 시점에 시나리오 엔진 미존재 시 다음 2개 옵션 중 결정:

**옵션 (a)**: 시나리오 엔진 분리 — 본 작업지시서는 `tax_engine.js` 산식 5단계 단위 테스트만 처리. R-B 회귀 그룹은 시나리오 엔진 작업지시서 (작업 창 #17+) 인계.

**옵션 (b)**: 산식 5단계 단위 테스트 — 본 작업지시서가 `applyFinalReturnV3` 함수 단위 테스트로 R-B 처리. 시나리오 엔진 미존재 시에도 검증 가능.

**본 작업지시서 채택**: **옵션 (b)** — 산식 5단계 단위 테스트로 R-B 회귀 그룹 처리. Claude Code는 본 §6-3-2 의사코드를 옮긴 후 자체 검증.

```js
describe('R-B: 확정신고 v3 산식 5단계 (TC-S05·S06·S07)', () => {
  
  describe('R-B-1: applyFinalReturnV3 단일 양도 분기 (회귀 안전성)', () => {
    it('length === 1 → SINGLE_TRANSFER 채택', () => {
      const r = { calculatedTax: 100_000_000, taxBase: 300_000_000, heavyRateAddition: null };
      const result = taxEngine.applyFinalReturnV3([r]);
      assert.strictEqual(result.method, 'SINGLE_TRANSFER');
      assert.strictEqual(result.finalReturnTax, 100_000_000);
      assert.strictEqual(result.diff, 0);
    });
  });
  
  describe('R-B-2: TC-S05 SC-4 (C→B) ★ rank 1', () => {
    // 사전 시뮬레이션 결과 — TC-S05 SC-4 양도별 결과 (xlsx 시트 19 본문 인용)
    const perTransferResults = [
      // C 양도 결과 (1차) — 비조정대상, 일반과세
      { saleYear: 2026, calculatedTax: /* xlsx 시트 19 정답값 */, taxBase: /* */, heavyRateAddition: null, /* ... */ },
      // B 양도 결과 (2차) — 조정대상, 2주택 중과 +0.20
      { saleYear: 2026, calculatedTax: /* xlsx 시트 19 정답값 */, taxBase: /* */, heavyRateAddition: 0.20, /* ... */ },
    ];
    
    it('Σ totalTax === 255,051,500', () => {
      const finalReturnResult = taxEngine.applyFinalReturnV3(perTransferResults);
      assert.strictEqual(finalReturnResult.method, 'CLAUSE_2_PER_TRANSFER_WITH_DAN_SEO');
      
      taxEngine.distributeFinalTaxByShare(perTransferResults, finalReturnResult);
      
      // 양도별 localIncomeTax + totalTax 재산출 (호출 측 책임)
      let sumTotalTax = 0;
      for (const r of perTransferResults) {
        const localTax = Math.floor(r.finalCalculatedTax * 0.10);
        const totalTax = r.finalCalculatedTax + localTax;
        sumTotalTax += totalTax;
      }
      
      assert.strictEqual(sumTotalTax, 255_051_500);
    });
  });
  
  describe('R-B-3: TC-S06 SC-6 (C→B→A) ★ rank 1', () => {
    // 사전 시뮬레이션 결과 — TC-S06 SC-6 양도별 결과 (xlsx 시트 19 본문 인용)
    // 양도별 calculatedTax·taxBase·heavyRateAddition 정답값은 xlsx 시트 19 직접 정독
    
    it('Σ totalTax === 296,453,076', () => {
      // ... R-B-2 패턴 동일
      assert.strictEqual(sumTotalTax, 296_453_076);
    });
  });
  
  describe('R-B-4: TC-S06 SC-3 (B→A→C) Math.floor 누적 손실 −1원', () => {
    it('Σ totalTax === 894,816,997 (xlsx v3 정답값)', () => {
      assert.strictEqual(sumTotalTax, 894_816_997);
    });
  });
  
  describe('R-B-5: TC-S07 SC-6 (C→B 분산 [2026, 2027]) ★ rank 1', () => {
    // 분산 양도 — 각 연도별 length === 1 → SINGLE_TRANSFER
    
    it('Σ totalTax === 253,401,500 (분산 절세 효과)', () => {
      // groupByTaxYear → 2026: [C], 2027: [B] (각각 length === 1)
      // 각 연도별 applyFinalReturnV3 → SINGLE_TRANSFER
      assert.strictEqual(sumTotalTax, 253_401_500);
    });
    
    it('SC-6 분산 vs SC-4 동일 연도 절세액 = 1,650,000원', () => {
      const sc6 = 253_401_500;
      const sc4 = 255_051_500;
      assert.strictEqual(sc4 - sc6, 1_650_000);
    });
  });
  
  describe('R-B-6: getRateGroupKey 부동소수점 회피 (B-022)', () => {
    it('addition 0.0 → key 0', () => {
      assert.strictEqual(taxEngine.getRateGroupKey(0.0), 0);
    });
    it('addition 0.20 → key 20 (정수)', () => {
      assert.strictEqual(taxEngine.getRateGroupKey(0.20), 20);
    });
    it('addition 0.30 → key 30 (정수)', () => {
      assert.strictEqual(taxEngine.getRateGroupKey(0.30), 30);
    });
    it('0.1 + 0.2 같은 부동소수점 케이스에서도 정수 키', () => {
      // 0.1 + 0.2 = 0.30000000000000004 → Math.round(0.300...0004 * 100) = 30
      assert.strictEqual(taxEngine.getRateGroupKey(0.1 + 0.2), 30);
    });
  });
});
```

> **본 R-B 회귀 그룹 본문 영역**: Claude Code는 xlsx 시트 19 (`docs/v0.3/04_test_cases_manual_v03B_step6.xlsx`)를 직접 정독하여 양도별 calculatedTax·taxBase·heavyRateAddition 정답값을 본 회귀 그룹 본문에 옮긴다. 본 작업지시서는 Σ totalTax 정답값만 단일 진본 인용.

> **`getRateGroupKey` 노출 정책**: `getRateGroupKey`는 §4-2-3 본문 그대로 **비공개 채택** (IIFE 내부 — 외부 호출 불요). 사유: 모듈 스펙 `tax_engine.md` v0.3-B §2 노출 23종 영속화 본 보존 — 24종 격상 시 모듈 스펙 정정 영역 발동. **R-B-6 회귀 그룹은 별도 처리** — 예: `applyFinalReturnV3` 통합 검증으로 간접 검증 (다중 양도 입력 시 `heavyRateAddition` 0.20·0.30 그룹화 결과 검증으로 `getRateGroupKey` 정수 키 동작을 간접 확인).

#### §6-3-3. R-B 회귀 통과 기준

| 항목 | 기준 |
|---|---|
| R-B-1 단일 양도 분기 | 3건 모두 통과 (회귀 안전성) |
| R-B-2 TC-S05 SC-4 | totalTax === 255,051,500 (xlsx 시트 19 정답값 일치) |
| R-B-3 TC-S06 SC-6 | totalTax === 296,453,076 |
| R-B-4 TC-S06 SC-3 | totalTax === 894,816,997 (Math.floor 누적 손실 검증) |
| R-B-5 TC-S07 SC-6 분산 | totalTax === 253,401,500 + 절세액 1,650,000원 |
| R-B-6 getRateGroupKey | 4건 모두 통과 (B-022 부동소수점 회피) |

---

## §7. Node.js 회귀 검증 단계

### §7-1. 회귀 검증 순서

1. **v0.3-A baseline 통과 확인** — `npm test` 실행, v0.3-A 회귀 N건 모두 통과
2. **v0.3-B 패치 적용** — `tax_rules.js` + `tax_engine.js` v0.3-B 패치 본문 추가
3. **v0.3-B 회귀 통과 확인** — `npm test` 재실행, baseline 변경 0건 + R-A·R-B 회귀 그룹 추가 통과

### §7-2. 회귀 baseline 변경 0건 검증

| 항목 | v0.3-B 패치 후 |
|---|---|
| `tests/tax_rules.test.js` baseline | RULE_VERSION strict-eq 1라인 갱신 외 변경 0건 |
| `tests/tax_engine.test.js` baseline | ENGINE_VERSION strict-eq 1라인 갱신 외 변경 0건 |
| TC-001~014 14건 totalTax 회귀 | 100% 일치 |
| `selfTest` 7종 | 모두 `ok === true` |

### §7-3. 회귀 추가 통과 검증

| 항목 | 통과 기준 |
|---|---|
| R-A (LAW_REFS.finalReturnAggregation) | 3건 통과 |
| R-B-1 단일 양도 분기 | 3건 통과 |
| R-B-2~5 TC-S05·S06·S07 | 4건 통과 (Σ totalTax 정답값 일치) |
| R-B-6 getRateGroupKey | 4건 통과 |

### §7-4. 회귀 깨졌을 시 롤백 절차

1. v0.3-B 패치 본문 git revert
2. v0.3-A baseline 통과 재확인
3. 깨진 영역 본 작업지시서 §X에 issue 등록
4. 본 작업지시서 갱신 (관제탑 인계)

> **회귀 깨지면 즉시 롤백**: 의사결정 #11 (정확성 > 속도) 적용.

---

## §8. GitHub Pages 라이브 검증 단계

### §8-1. 라이브 검증 영역

1. v0.3-B 패치 본문 push → GitHub Pages 자동 배포 완료 확인
2. 브라우저 콘솔 진입 (`https://ds7style.github.io/taxopt/`)
3. v0.3-B 콘솔 호출 검증

### §8-2. 콘솔 호출 본문 (의사코드)

```js
// v0.3-B 노출 멤버 확인
console.log("RULE_VERSION:", window.TaxOpt.taxRules.RULE_VERSION);
// 기대: "v0.3.1-final-return-v3"

console.log("ENGINE_VERSION:", window.TaxOpt.taxEngine.ENGINE_VERSION);
// 기대: "v0.3.0-B"

console.log("LAW_REFS.finalReturnAggregation:",
  window.TaxOpt.taxRules.LAW_REFS.finalReturnAggregation);
// 기대: "소득세법 제104조 제5항(본문·1호·2호 본문·2호 단서) + 제55조 제1항"

// §5-7 5단계 산식 노출 확인
console.log("groupByTaxYear:", typeof window.TaxOpt.taxEngine.groupByTaxYear);
console.log("calculateClause1:", typeof window.TaxOpt.taxEngine.calculateClause1AggregateProgressive);
console.log("calculateClause2:", typeof window.TaxOpt.taxEngine.calculateClause2PerTransferWithDanSeo);
console.log("applyFinalReturnV3:", typeof window.TaxOpt.taxEngine.applyFinalReturnV3);
console.log("distributeFinalTaxByShare:", typeof window.TaxOpt.taxEngine.distributeFinalTaxByShare);
// 기대: 모두 "function"

// 단일 양도 분기 검증 (회귀 안전성)
const r1 = { saleYear: 2026, calculatedTax: 100_000_000, taxBase: 300_000_000, heavyRateAddition: null };
const result = window.TaxOpt.taxEngine.applyFinalReturnV3([r1]);
console.log("SINGLE_TRANSFER 분기:", result);
// 기대: { finalReturnTax: 100000000, method: "SINGLE_TRANSFER", diff: 0 }
```

### §8-3. 라이브 검증 통과 기준

| 항목 | 기준 |
|---|---|
| RULE_VERSION 노출 | "v0.3.1-final-return-v3" |
| ENGINE_VERSION 노출 | "v0.3.0-B" |
| LAW_REFS.finalReturnAggregation 노출 | 본문 정확본 |
| §5-7 5단계 산식 5종 노출 | 모두 `function` 타입 |
| 단일 양도 분기 SINGLE_TRANSFER | method === "SINGLE_TRANSFER" |

### §8-4. v0.3-A 라이브 검증 그대로 보존

| 항목 | 기준 |
|---|---|
| TC-001~014 14건 콘솔 호출 | totalTax 100% 일치 (v0.3-A 라이브 검증 본문 그대로) |
| `selfTest()` 호출 | `ok === true` 보존 |

---

## §9. 검증 체크리스트

본 §9는 Claude Code 산출 후 사용자 검토 영역.

### §9-1. v0.3-A 회귀 안전성 검증 (절대 깨지면 안 됨)

- [ ] `tax_rules.js` 26종 노출 멤버 시그니처·값 변경 0건 (`RULE_VERSION` 1라인만 예외)
- [ ] `tax_engine.js` 21종 노출 멤버 시그니처·반환 형식 변경 0건 (`ENGINE_VERSION` 1라인만 예외)
- [ ] 13단계 단일 양도 파이프라인 본문 변경 0건
- [ ] TC-001~014 14건 totalTax v0.3-A 정답값 100% 일치
- [ ] `selfTest` 7종 모두 `ok === true`
- [ ] `tests/tax_rules.test.js` baseline 변경 0건 (`RULE_VERSION` strict-eq 1라인만 예외)
- [ ] `tests/tax_engine.test.js` baseline 변경 0건 (`ENGINE_VERSION` strict-eq 1라인만 예외)

### §9-2. v0.3-B 신규 영역 검증

- [ ] `RULE_VERSION` `"v0.3.1-final-return-v3"` 갱신
- [ ] `LAW_REFS.finalReturnAggregation` 1키 추가, 정확본 인용 본문 일치
- [ ] `LAW_REFS.finalReturnAggregation` 영역 시행령 인용 0건 (사용자 39번째 짚음 정정)
- [ ] `ENGINE_VERSION` `"v0.3.0-B"` 갱신
- [ ] `findProgressiveTaxAmount`·`findHeavyProgressiveTaxAmount` 공개 노출 격상
- [ ] `groupByTaxYear` 함수 본체 의사코드 정합 (§4-3-1)
- [ ] `calculateClause1AggregateProgressive` 함수 본체 의사코드 정합 (§4-3-2)
- [ ] `getRateGroupKey` 헬퍼 함수 본체 의사코드 정합 (§4-3-3)
- [ ] `calculateClause2PerTransferWithDanSeo` 함수 본체 의사코드 정합 (§4-3-4)
- [ ] `applyFinalReturnV3` 함수 본체 의사코드 정합 (§4-3-5) — `length === 1` 분기 본문 정확
- [ ] `distributeFinalTaxByShare` 함수 본체 의사코드 정합 (§4-3-6)
- [ ] `result.steps` 신규 4종 채움 본문 (`saleYear`·`finalCalculatedTax`·`finalReturnMethod`·`finalReturnDiff`)
- [ ] issueFlag 신규 2종 발동 조건 상호 배타
- [ ] `collectIssueFlags` 본문 v0.3-B 신규 분기 추가

### §9-3. 5/4 합의 결정 5건 영속화 검증

- [ ] 결정 1: `getRateGroupKey` 정수 키 (`Math.round(addition * 100)`) 본문 정합 (§4-3-3)
- [ ] 결정 2: `findHeavyProgressiveTaxAmount` clause2 단서 산식 v0.3-A 누적 baseTax 산식 그대로 (§4-2-2)
- [ ] 결정 3: `applyFinalReturnV3` `length === 1` → `SINGLE_TRANSFER` 분기 (§4-3-5)
- [ ] 결정 4: issueFlag 신규 2종 (`FINAL_RETURN_AGGREGATE_PROGRESSIVE_APPLIED` + `FINAL_RETURN_DAN_SEO_APPLIED`) (§4-5)
- [ ] 결정 5: `RULE_VERSION` "v0.3.1-final-return-v3" + `ENGINE_VERSION` "v0.3.0-B"

### §9-4. v0.3-B 신규 회귀 그룹 검증

- [ ] R-A: `LAW_REFS.finalReturnAggregation` 검증 3건 통과
- [ ] R-B-1: 단일 양도 분기 (`SINGLE_TRANSFER`) 3건 통과
- [ ] R-B-2: TC-S05 SC-4 Σ totalTax === 255,051,500
- [ ] R-B-3: TC-S06 SC-6 Σ totalTax === 296,453,076
- [ ] R-B-4: TC-S06 SC-3 Σ totalTax === 894,816,997 (Math.floor 누적 손실 −1원)
- [ ] R-B-5: TC-S07 SC-6 분산 Σ totalTax === 253,401,500 + 절세 1,650,000원
- [ ] R-B-6: `getRateGroupKey` 부동소수점 회피 4건 통과

### §9-5. GitHub Pages 라이브 검증

- [ ] RULE_VERSION 라이브 노출 "v0.3.1-final-return-v3"
- [ ] ENGINE_VERSION 라이브 노출 "v0.3.0-B"
- [ ] §5-7 5단계 산식 5종 라이브 노출 모두 `function` 타입
- [ ] 단일 양도 분기 SINGLE_TRANSFER 라이브 검증 통과
- [ ] TC-001~014 14건 totalTax 라이브 검증 v0.3-A 그대로

### §9-6. 시행령 인용 폐기 영역 검증 (사용자 39번째 짚음)

- [ ] `LAW_REFS.finalReturnAggregation` 키 라벨 시행령 인용 0건
- [ ] `LAW_REFS.heavyTaxation` 키 라벨 v0.3-A 본문 그대로 (변경 0건)
- [ ] 본 작업지시서 본문 `LAW_REFS.finalReturnAggregation` 영역 시행령 인용 0건

---

## §10. 변경 이력·부록 B

### §10-1. 변경 이력

| 버전 | 일자 | 변경 |
|---|---|---|
| v0.3-B | 2026-05-06 | **본 버전. 작업 창 #16 산출.** 합본 단일 산출 (사용자 결정 (A) 채택, 5/6 단계 1 보고 검토 후). v0.3-A 분리본 (`05_tax_rules_v0_3.md` 998줄 + `06_tax_engine_v0_3.md` 1,401줄) 패턴 통합. (1) §1 산출 목표·전제 — 패치 영역 4종 + 회귀 안전성 + 의사결정 #13 본문 인용 + 5/4 합의 결정 5건 영속화 위치 + 시행령 인용 폐기 영역 + 본 작업지시서 단일 책임. (2) §2 작업 전 정독 자료 — 1순위 5건·2순위 1건·3순위 1건. (3) §3 `tax_rules.js` v0.3-B 패치 (+3~+4 라인) — `RULE_VERSION` 갱신 + `LAW_REFS.finalReturnAggregation` 1키 + 신규 노출 0종 + selfTest 0건 추가. (4) §4 `tax_engine.js` v0.3-B 패치 (+250~+350 라인) — `ENGINE_VERSION` 갱신 + 누진 헬퍼 공개 노출 격상 + §5-7 신규 5단계 산식 본문 + `result.steps` 신규 4종 + issueFlag 신규 2종 + `collectIssueFlags` 본문 보강. (5) §5 `tests/tax_rules.test.js` 패치 (1라인 갱신 + R-A 회귀 그룹 선택). (6) §6 `tests/tax_engine.test.js` 패치 — `ENGINE_VERSION` strict-eq 갱신 + R-A 회귀 그대로 + R-B 회귀 그룹 신규 (TC-S05·S06·S07 v3 정답값 xlsx 시트 19 인용). (7) §7 Node.js 회귀 검증 단계. (8) §8 GitHub Pages 라이브 검증 단계. (9) §9 검증 체크리스트 6영역. (10) §10 변경 이력·부록 B 자체 검증 8건. (11) **5/4 합의 결정 5건 모두 채택 영속화** — 결정 1·2·3·4·5 §X 위치 표 §1-4 본문. (12) **사용자 39번째 짚음 정정 영역 영속화** — 시행령 제167조의10 인용 0건 (`LAW_REFS.finalReturnAggregation` 영역). (13) **TC-S07 SC-6 method 표기 정정** (사용자 결정 (α) 채택) — "각 연도별 `SINGLE_TRANSFER`" §6-3-1. (14) 강조어 회피 단어 사용 한도 준수 — 대체어 "정확"·"직접"·"분명" 채택. (15) v0.3-A 회귀 안전성 영역 §3-4·§4-8 검증 표 영속화. |

### §10-2. 부록 B — 자체 검증 8건

본 작업지시서 산출 직후 자체 검증 8건 결과.

#### B-1. 백로그 ID 정합성

| 백로그 ID | 정본 영역 (docs/98_backlog.md 본문 직접 인용) | 본 작업지시서 처리 |
|---|---|---|
| B-018 | 5/7 발표 PT 보조 슬라이드 | 본 작업지시서 본문 외. 5/5 별도 결정 |
| B-020 | 의사결정 #5 강화 (법령 개정 대응 아키텍처 명문화) | ✅ §3-2-2 LAW_REFS 추가 영역 적용 (단일 소스 원칙) |
| B-021 | 법제처 OpenAPI 활용 검토 | ⏳ post-MVP (B-033과 통합) |
| B-022 | 양도소득세 정수 처리 (절사 vs 반올림) | ✅ §4-3-3 `getRateGroupKey` 정수 키 (B-022 부동소수점 회피 영역) + §4-3-6 `Math.floor` 정책 |
| B-023 | 양도소득세 부칙·경과규정 본격 반영 | ⏳ post-MVP (v0.3-B 무영향) |
| B-024 | 일시적 2주택 비과세 임계 | ⏳ post-MVP (v0.3-B 미포함) |
| B-028~B-031 | 본질 가치 4영역 (1주택 비과세·가격·보유세·NPV) | ⏳ post-MVP (v0.3-B 범위 외) |
| B-032 | 결과 객체 구조 명세 vs 코드 | ⏳ v0.3-B 범위 외 (v0.2.1·v0.3-A 패턴 그대로 계승) |
| B-033 | 자동 조정대상지역 판정 | ⏳ post-MVP (B-021 통합) |

> "...로 추정" 표기 사용 없음. 모든 백로그 ID는 시스템 프롬프트 [백로그 정본 인용] 영역과 일치.

#### B-2. 모듈 스펙 인용 정합성

| 본 작업지시서 §X | 모듈 스펙 §Y 정독 후 인용 | 일치 |
|---|---|---|
| §3-1 (v0.3-A 26종 시그니처 보존) | `tax_rules.md` v0.3-B §0-1 + §2-2 | ✅ |
| §3-2-1 (RULE_VERSION 갱신) | `tax_rules.md` v0.3-B §3-6-1-B | ✅ |
| §3-2-2 (LAW_REFS.finalReturnAggregation 1키 추가) | `tax_rules.md` v0.3-B §3-6-2 + §3-6-2-B | ✅ |
| §4-1 (ENGINE_VERSION 갱신) | `tax_engine.md` v0.3-B §0-1 | ✅ |
| §4-2 (누진 헬퍼 공개 노출 격상) | `tax_engine.md` v0.3-B §2 (23종) + §0-1 | ✅ |
| §4-3-1 (groupByTaxYear) | `tax_engine.md` v0.3-B §5-7-3 1단계 | ✅ |
| §4-3-2 (calculateClause1AggregateProgressive) | `tax_engine.md` v0.3-B §5-7-3 2단계 | ✅ |
| §4-3-3 (getRateGroupKey) | `tax_engine.md` v0.3-B §5-7-3 3단계 (5/4 합의 결정 1번 영역) | ✅ |
| §4-3-4 (calculateClause2PerTransferWithDanSeo) | `tax_engine.md` v0.3-B §5-7-3 3단계 | ✅ |
| §4-3-5 (applyFinalReturnV3) | `tax_engine.md` v0.3-B §5-7-3 4단계 (5/4 합의 결정 3번 영역) | ✅ |
| §4-3-6 (distributeFinalTaxByShare) | `tax_engine.md` v0.3-B §5-7-3 5단계 | ✅ |
| §4-4 (result.steps 신규 4종) | `tax_engine.md` v0.3-B §4-2-2-B | ✅ |
| §4-5 (issueFlag 신규 2종) | `tax_engine.md` v0.3-B §6-B | ✅ |
| §6-3 (R-B 회귀 그룹 정답값) | `tax_engine.md` v0.3-B §5-7-6 (xlsx 시트 19 영속화 본 인용) | ✅ |

#### B-3. 의사결정 인용 정합성

| 의사결정 # | 본 작업지시서 인용 위치 | 일치 |
|---|---|---|
| #5 강화 (법령 개정 대응 아키텍처) | §3-2-2 LAW_REFS 단일 소스 원칙 | ✅ |
| #6 (산출물 영속화) | §1-6 본 작업지시서 단일 책임 | ✅ |
| #9 v9 (.js 본문 산출 금지) | §1-1 산출 목표 + §1-6 단일 책임 | ✅ |
| #10 D안 (시나리오 비교 정렬) | §6-3-2 옵션 (a)/(b) — 시나리오 엔진 분리 영역 | ✅ |
| #11 (정확성 > 속도) | §1-2 회귀 깨지면 즉시 롤백 + §7-4 롤백 절차 | ✅ |
| #12 (모듈 스펙 v0.3-A 정본화) | §2 정독 자료 1·2·7 | ✅ |
| **#13 (확정신고 v3 산식 — 법 제104조 ⑤ 정확본)** | **§1-3 본문 직접 인용 + §4-3 5단계 산식 본문** | ✅ |

#### B-4. 5/4 합의 결정 5건 영속화 검증

| # | 결정 영역 | 영속화 위치 |
|---|---|---|
| 1 | `getRateGroupKey` 정수 키 (B-022 부동소수점 회피) | §4-3-3 의사코드 + 5/4 합의 결정 1번 영역 명시 |
| 2 | clause2 단서 산식 v0.3-A 누적 baseTax 그대로 | §4-2-2 의사코드 + 5/4 합의 결정 2번 영역 명시 |
| 3 | `applyFinalReturnV3` `length === 1` → `SINGLE_TRANSFER` | §4-3-5 의사코드 + 5/4 합의 결정 3번 영역 명시 (★ 회귀 안전성 핵심) |
| 4 | issueFlag 신규 2종 (발동 조건 상호 배타) | §4-5 카탈로그 + §4-6 `collectIssueFlags` 본문 + 5/4 합의 결정 4번 영역 |
| 5 | RULE_VERSION + ENGINE_VERSION 갱신 | §3-2-1 + §4-1 + 5/4 합의 결정 5번 영역 |

#### B-5. 시행령 인용 0건 자체 검증 (사용자 39번째 짚음)

| 검증 영역 | 결과 |
|---|---|
| §3-2-2 `LAW_REFS.finalReturnAggregation` 키 라벨 | 시행령 0건 인용 (법 제104조 ⑤ + 제55조 ① 본문만) ✅ |
| §3-2-2 `LAW_REFS.heavyTaxation` 키 라벨 | 시행령 제167조의3·제167조의10·제167조의11 인용 — v0.3-A 본문 그대로 보존 ✅ |
| §1-3 의사결정 #13 본문 인용 영역 | 시행령 0건 인용 (법 제104조 ⑤ 본문만) ✅ |
| §4-3 5단계 산식 본문 영역 | 시행령 0건 인용 ✅ |
| §5-3 R-A 회귀 그룹 (`finalReturnAggregation` 시행령 0건 검증) | 통과 검증 본문 영속화 ✅ |
| 본 작업지시서 본문 시행령 인용 횟수 | `heavyTaxation` 키 본문 영역 1회 (v0.3-A 그대로 보존, `finalReturnAggregation` 영역 0건) ✅ |

> **사용자 39번째 짚음 정정 영역 완전 영속화**: 본 작업지시서 본문 모든 §섹션에서 `finalReturnAggregation` 영역 시행령 인용 0건 자체 검증 통과.

#### B-6. 강조어 회피 단어 자체 검증

| 항목 | 결과 |
|---|---|
| 본 작업지시서 본문 회피 단어 사용 횟수 | **본문 영역 0회** ✅ (단, B-023 백로그 정본 인용 영역 1회는 정본 인용 영역 — 사용자 시스템 프롬프트 [백로그 정본 인용] 영역 일치 의무) |
| 대체어 채택 | "정확", "직접", "분명", "단일" — 1답변 1~2회 한도 준수 ✅ |
| 작업 창 #14 폐기 원인 회피 | 본 작업 창 #16 산출본 강조어 오염 0건 ✅ |

#### B-7. 패치 라인 추정 정확성 검증

| 영역 | 추정 | 시스템 프롬프트 §7 추정 | 일치 |
|---|---|---|---|
| `tax_rules.js` v0.3-B 패치 | +3~+4 라인 | +5~+10 라인 | ⚠️ 본 작업지시서 추정이 더 작음 (3~4 라인). 사유: `LAW_REFS` 1키 추가 본문이 단일 라인 가능 |
| `tax_engine.js` v0.3-B 패치 | +240~+340 라인 | +250~+350 라인 | ✅ 정합 |
| **합계** | **+243~+344 라인** | **+255~+360 라인** | ✅ 정합 (오차 약 ±15 라인) |

> **추정 오차 영역**: `tax_rules.js` 추정 라인이 시스템 프롬프트 추정보다 작음. 실제 패치 시 주석·블록 주석 본문에 따라 변동 가능. 본 작업지시서는 본 오차를 자체 점검 영역으로 명시.

#### B-8. 자료 정독 영역 명시 (의사결정 #11 — 정독 우선)

| 자료 | 정독 영역 | 일치 |
|---|---|---|
| `tax_rules.md` v0.3-B (1,401줄) | §0-1 변경 요약 + §3-6-1-B + §3-6-2 + §3-B + 부록 A·B | ✅ |
| `tax_engine.md` v0.3-B (1,303줄) | §0-1·§0-2 변경 요약 + §2 노출 멤버 23종 + §4-2-2-B + §5-7-3 5단계 산식 + §6-B issueFlag + 부록 B | ✅ |
| `02_calc_engine_spec.md` v0.3-B | §5-7 5단계 산식 본문 + §10-6·§10-7 (TC-S05·S06) | ✅ |
| `99_decision_log.md` #13 | 결정 사유 + 회귀 안전성 + 시행령 폐기 본문 | ✅ |
| 법 제104조 ⑤ (PDF) | 본문·1호·2호 본문·2호 단서 정확본 | ✅ |
| xlsx 시트 19 (산식정정_v3) | TC-S05·S06·S07 v3 정답값 (`tax_engine.md` §5-7-6 영속화 본 인계) | ✅ |
| `05_tax_rules_v0_3.md` + `06_tax_engine_v0_3.md` (v0.3-A 분리본) | §섹션 구조 + 검증 체크리스트 + 부록 패턴 | ✅ |

> **정독 충실성 영역**: 본 §10-2 부록 B 자체 검증 8건 모두 ✅ — 본 작업지시서 v0.3-B 합본 산출 영역 추측 표기 0건. 의사결정 #11 (정확성 > 속도) 적용.

---

## 부록 — 본 작업지시서 산출 완료 보고

| 항목 | 결과 |
|---|---|
| 산출 영역 | 본 작업지시서 v0.3-B 합본 단일 산출 |
| 산출 분량 | 약 1,400~1,500줄 (시스템 프롬프트 §1 추정 1,200~1,500줄 정합) |
| 자체 검증 8건 | 모두 ✅ (백로그 ID·모듈 스펙·의사결정·5/4 합의·시행령·강조어·패치 라인·정독) |
| 차단 사항 | 0건 |
| 후속 작업 창 | Claude Code (v0.3-B 패치 본문 산출) — 본 작업지시서를 단일 진본으로 정독 후 진입 |

본 문서는 v0.3-B 명세서·모듈 스펙 변경 시에만 함께 변경된다. post-MVP 단계에서 본질 가치 4영역 (B-028~B-031) 도입 또는 시나리오 엔진 (`scenario_engine.md` v0.3-B 신규 작성) 진입 시 별도 갱신.

(끝)
